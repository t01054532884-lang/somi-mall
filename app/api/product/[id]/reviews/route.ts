import {getMemberSession} from '@/app/google-auth';import {getD1} from '@/db';
type C={params:Promise<{id:string}>};export async function POST(request:Request,context:C){const member=await getMemberSession(),{id}=await context.params;if(!member)return go(request,id,'login','reviews');try{const form=await request.formData(),rating=Number(form.get('rating')),rawContent=form.get('content'),content=typeof rawContent==='string'?rawContent.trim():'';if(!Number.isInteger(rating)||rating<1||rating>5||content.length<10||content.length>1000)throw new Error('invalid review');await getD1().prepare(`INSERT INTO reviews (id,product_id,member_id,member_name,rating,content,created_at) VALUES (?,?,?,?,?,?,CURRENT_TIMESTAMP) ON CONFLICT(product_id,member_id) DO UPDATE SET rating=excluded.rating,content=excluded.content,member_name=excluded.member_name,created_at=CURRENT_TIMESTAMP`).bind(crypto.randomUUID(),id,member.memberId,member.displayName,rating,content).run();return go(request,id,'reviewed','reviews')}catch(error){console.error('Review failed',error);return go(request,id,'error','reviews')}}
function go(request:Request,id:string,status:string,hash:string){const url=new URL(`/product/${encodeURIComponent(id)}`,request.url);url.searchParams.set('status',status);url.hash=hash;return Response.redirect(url,303)}
