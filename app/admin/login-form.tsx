'use client';
import {useState} from 'react';
export default function LoginForm(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const data=new FormData(e.currentTarget);const res=await fetch('/api/admin-login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({id:data.get('id'),password:data.get('password')})});setBusy(false);if(res.ok)location.reload();else setError((await res.json()).error)}return <form onSubmit={submit}><label className="field">관리자 아이디<input name="id" autoComplete="username" required/></label><label className="field">비밀번호<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<p className="error">{error}</p>}<button className="solid" disabled={busy}>{busy?'확인 중…':'관리자 로그인'}</button></form>}
